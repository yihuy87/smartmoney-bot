telegram:
  enabled: false     # kalau mau pakai Telegram, ganti ke true

# tidak pakai spot onchain dulu
evm_chains: []

perp_platforms:
  - name: "hyperliquid"
    base_url_env: "HYPERLIQUID_BASE_URL"

thresholds:
  min_wallet_score: 70
  min_spot_size_usd: 1000       # sekarang tidak terpakai (nggak ada spot)
  min_liquidity_usd: 200000     # sama, bisa diabaikan dulu
  min_perp_size_usd: 5000
  risk_per_trade_default: 0.01   # 1% risk per trade

# seed wallet yang ingin kamu ikuti
tracked_wallets:
  - address: "0xMOCKWALLET"
    initial_score: 85
    initial_tier: "S"
  # nanti ganti/tambah wallet beneran di sini

  discovery:
  top_n: 50
  min_account_value: 20000
